{
  "schema": 2,
  "gate": "G4/G5 corrective evidence",
  "privacy": "structural-only; no paths, prompts, transcripts, identifiers, model prose, credentials, or account configuration",
  "landed_g3": {"fixture": "claude-watchtower-g3-2.1.240.json", "reviewed_head": "7ede79ccf2c06b3db87049121b0cfd1820cb0c14"},
  "behavioral_evidence": [
    {"case": "normal legacy wake refusal", "interface": "rzr-watch.sh --once --wake", "result": "refused before generation, delivery, or Herdr prompt; explicit diagnostic marker required"},
    {"case": "Pi authority readiness", "interface": "real adapter registration callback", "result": "connected readiness follows persistent authority activation"},
    {"case": "managed Pi crew", "interface": "Pi socket producer", "result": "session.register, turn.start, turn.stop carry role=crew and task identity without watchtower registration"},
    {"case": "autostart", "interface": "managed Pi and supported-Claude launch/spawn/resume", "result": "concurrent-safe monitor start waits for health and launch fails closed"},
    {"case": "completion clustering", "interface": "native daemon plus adapter", "result": "two quiescent task completions persist separately and yield one fixed wake/reconcile snapshot"},
    {"case": "metadata isolation", "interface": "resident membership plus simultaneous settlement", "result": "repeated unrelated metadata writes cause zero adapter epoch churn while settlement remains delivered"},
    {"case": "delivered-unacked restart", "interface": "native daemon plus adapter", "result": "same generation reoffered after restart and ACKed only by reconcile"},
    {"case": "five restarts", "interface": "native daemon", "result": "zero synthetic generations/completions"},
    {"case": "health", "interface": "monitor status --json", "result": "down/disconnected, per-driver delivery/error, deferred, delivered-unacked, retrying, unknown, and spool backlog are structurally distinguishable; pending_count excludes fully ACKed tasks"},
    {"case": "rollback", "interface": "rozoro rollback --driver", "result": "unequal cursors refuse; equal cursors tombstone daemon authority before marker removal; daemon is stopped only afterward"},
    {"case": "upgrade preservation", "interface": "schema migration and cutover refusal", "result": "dirty old ledger refuses; task, brief, append-only handoff, session link, and ACK cursors remain unchanged"}
  ],
  "executable_sources": ["tests/cutover.bats", "tests/pi-cutover-additional.test.ts", "tests/pi-event-bus-adapter.test.ts", "tests/cli-event-bus.bats", "tests/python/test_herdr.py", "tests/python/test_server.py", "tests/python/test_store.py"]
}
