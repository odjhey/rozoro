#!/usr/bin/env sh
[ -z "${FAKE_CLAUDE_LOG:-}" ] || printf '%s\n' "$*" >> "$FAKE_CLAUDE_LOG"
[ "${1:-}" = --version ] && { echo "${FAKE_CLAUDE_VERSION:-2.1.240} (Claude Code)"; exit 0; }
[ -z "${FAKE_CLAUDE_SLEEP:-}" ] || sleep "$FAKE_CLAUDE_SLEEP"
exit 0
